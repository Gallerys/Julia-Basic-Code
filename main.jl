# This is a comment in Julia. It's ignored by the Julia compiler and used for documentation.

# Define a function that prints a greeting message
function greet(name)
    println("Hello, $name!")
end

# Call the greet function with different names
greet("Alice")
greet("Bob")
